<div> 
    <h1> <%= @student.first_name %> <%= @student.last_name %> </h1> <h3> (<%= @student.age %> years old) </h3>
    <img src="<%= @student.pic %>">
</div>
<div>
    <h3><a href="/cohorts/<%= @student_cohort.id%>"> <%= @student_cohort.name %> </h3>
</div>
